import React from 'react'
import PropTypes from 'prop-types'
import './{{file}}.css'

const {{file}} = props => {
  return (
    <div className="{{file|paramCase}}">

    </div>
  )
}

{{file}}.propTypes = {

}

export default {{file}}

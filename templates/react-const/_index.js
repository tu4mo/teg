import React, { PropTypes } from 'react'

import './{{file}}.css'

const {{file}} = (props) => {
  return (
    <div className="{{file}}">

    </div>
  )
}

{{file}}.propTypes = {

}

export default {{file}}

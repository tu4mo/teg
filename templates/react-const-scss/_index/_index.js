import React from 'react'
import PropTypes from 'prop-types'
import './{{file}}.scss'

const {{file}} = props => {
  return (
    <div className="{{file|paramCase}}">

    </div>
  )
}

{{file}}.propTypes = {

}

export default {{file}}
